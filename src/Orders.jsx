import React, { Component } from "react";
import { Link } from "react-router-dom";
import { connect } from "react-redux";

class Orders extends Component {
  handleProfile = () => {};
  render = () => {
    return <div className="headerforms"> Orders </div>;
  };
}

export default Orders;
